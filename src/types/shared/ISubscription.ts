
export enum SubscriptionEnum {
    free = "free",
    pro = "pro",
    prem = "prem",
}

export enum SubscriptionPeriodServer { Annual = "Y", Monthly = "M", Unknown = ""}

export enum SubscriptionPeriod {Annual = "Annual", Monthly = "Monthly"};
